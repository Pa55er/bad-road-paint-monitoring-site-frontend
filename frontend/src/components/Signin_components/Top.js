import { Link } from 'react-router-dom';
import './Top.css';

export default function Top() {
    return (
        <div id='component_1'>
            <div id='Rectangle_5'/>
            <Link to="/signup" id='text_1'>don’t have an account? Create one now.</Link>
        </div>
    );
}
